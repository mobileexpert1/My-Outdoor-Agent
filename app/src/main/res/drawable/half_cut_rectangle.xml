<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background color -->
<!--    <solid android:color="@android:color/holo_red_dark"/>-->
    <solid android:color="@color/royal_red"/>

    <!-- Border color and width -->
   <!-- <stroke
        android:width="2dp"
        android:color="@color/red_pre_approval" />-->

    <!-- Rounded corners -->
    <corners
        android:topLeftRadius="@dimen/_11sdp"
        android:topRightRadius="@dimen/_11sdp" />

    <!-- Padding (optional, adds space inside the border) -->
    <padding
        android:left="@dimen/_12sdp"
        android:top="@dimen/_12sdp"
        android:right="@dimen/_12sdp"
        android:bottom="@dimen/_12sdp" />

</shape>
